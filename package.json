{
  "name": "layouter",
  "version": "1.0.0",
  "scripts": {
    "dev": "run-p \"build -- --watch\" serve",
    "build": "tsc && cp index.html dist",
    "serve": "serve docs"
  },
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "typescript": "^4.6.3"
  }
}

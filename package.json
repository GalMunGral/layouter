{
  "name": "layouter",
  "version": "1.0.0",
  "scripts": {
    "dev": "run-p \"build -- --watch\" serve",
    "build": "tsc",
    "serve": "serve docs",
    "clean": "rm -rf docs/**/*.js"
  },
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "typescript": "^4.6.3"
  }
}
